import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'a-forca-admin',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
